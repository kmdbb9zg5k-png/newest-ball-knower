import React,{useEffect,useRef,useState}from'react';
import{ArrowLeft,Flame,RotateCcw,Trophy}from'lucide-react';
import{GauntletProgress,GauntletScenario,loadGauntletProgress,recordGauntletAnswer,recordGauntletRun,saveGauntletProgress,shouldEliminateGauntletRun}from'./gauntletEngine';
import{ModalPortal}from'./ModalPortal';
import{trackBallKnowerEvent}from'./analytics';

const XP_BY_TIER={ROOKIE:10,PRO:20,'ALL-PRO':35,'HALL OF FAME':50}as const;
type Props={scenarios:GauntletScenario[];title:string;runKey:string;userId?:string;dailyDate?:string;onClose:()=>void;onProgress:(value:GauntletProgress)=>void;onReplay?:()=>void};

export const GauntletPlayModal:React.FC<Props>=({scenarios,title,runKey,userId,dailyDate,onClose,onProgress,onReplay})=>{
 const[index,setIndex]=useState(0);const[score,setScore]=useState(0);const[selected,setSelected]=useState<number|null>(null);const[eliminated,setEliminated]=useState(false);const recorded=useRef(false);
 const complete=index>=scenarios.length;const challenge=scenarios[Math.min(index,scenarios.length-1)];const progress=loadGauntletProgress(userId);
 const choose=(choice:number)=>{if(selected!==null||complete)return;const correct=choice===challenge.correct;setSelected(choice);if(correct)setScore(value=>value+1);else if(shouldEliminateGauntletRun(challenge.mode,correct,dailyDate))setEliminated(true);const next=recordGauntletAnswer(loadGauntletProgress(userId),correct,XP_BY_TIER[challenge.tier]);saveGauntletProgress(next,userId);onProgress(next);trackBallKnowerEvent('Gauntlet Challenge Answered',{mode:challenge.mode,tier:challenge.tier,correct,daily:Boolean(dailyDate),family:challenge.family});};
 const next=()=>{if(eliminated){setIndex(scenarios.length);return;}setIndex(value=>value+1);setSelected(null);};
 useEffect(()=>{if(!complete||recorded.current)return;recorded.current=true;const nextProgress=recordGauntletRun(loadGauntletProgress(userId),runKey,score,scenarios.length,dailyDate);saveGauntletProgress(nextProgress,userId);onProgress(nextProgress);trackBallKnowerEvent('Gauntlet Run Completed',{title,score,total:scenarios.length,daily:Boolean(dailyDate)});},[complete,dailyDate,onProgress,runKey,scenarios.length,score,title,userId]);
 return <ModalPortal><div role="dialog" aria-modal="true" aria-label={title} className="fixed inset-0 z-[9999] overflow-y-auto overscroll-contain bg-[#05070a] px-3 pb-[max(1.5rem,env(safe-area-inset-bottom))] pt-[max(.75rem,env(safe-area-inset-top))] text-white [-webkit-overflow-scrolling:touch]"><div className="mx-auto max-w-2xl"><header className="flex min-h-12 items-center justify-between gap-3"><button onClick={onClose} className="inline-flex min-h-11 items-center gap-2 text-[10px] font-black"><ArrowLeft size={16}/> EXIT</button><div className="text-center text-[10px] font-black tracking-widest text-fuchsia-300">{title}</div><div className="text-right text-[10px] font-black">{score} PTS</div></header>{complete?<section className="mt-5 rounded-3xl border border-fuchsia-400/25 bg-[radial-gradient(circle_at_50%_0%,rgba(217,70,239,.2),transparent_42%),#0b0e13] p-7 text-center"><Trophy className="mx-auto h-14 w-14 text-fuchsia-300"/><div className="mt-3 text-[10px] font-black tracking-widest text-fuchsia-300">{eliminated?'SURVIVOR RUN ENDED':'GAUNTLET COMPLETE'}</div><h2 className="mt-2 text-5xl font-black">{score} / {scenarios.length}</h2><div className="mt-4 grid grid-cols-3 gap-2"><Stat label="Streak" value={String(loadGauntletProgress(userId).currentStreak)}/><Stat label="Best" value={String(loadGauntletProgress(userId).highScores[runKey]||score)}/><Stat label="Level" value={String(loadGauntletProgress(userId).level)}/></div><div className="mt-5 grid gap-2 sm:grid-cols-2">{onReplay&&<button onClick={onReplay} className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-fuchsia-400 font-black text-black"><RotateCcw size={16}/> NEW RANDOM ROUND</button>}<button onClick={onClose} className="min-h-12 rounded-xl border border-white/10 font-black">BACK TO THE GAUNTLET</button></div></section>:<><div className="mt-3 flex items-center justify-between text-[9px] font-black text-zinc-500"><span>{challenge.mode} · {challenge.tier}</span><span className="inline-flex items-center gap-1"><Flame size={12} className="text-orange-400"/>{progress.currentStreak} STREAK</span></div><div className="mt-2 h-1 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-fuchsia-400 transition-all" style={{width:`${((index+1)/scenarios.length)*100}%`}}/></div><section className="mt-4 rounded-3xl border border-white/10 bg-[#0b0e13] p-5"><div className="text-[9px] font-black tracking-widest text-fuchsia-300">CHALLENGE {index+1} OF {scenarios.length}</div><div className="mt-3 rounded-xl bg-fuchsia-400/[.06] p-3 text-xs font-bold leading-5 text-zinc-300">{challenge.context}</div><h2 className="mt-4 text-2xl font-black leading-tight">{challenge.prompt}</h2><div className="mt-4 grid gap-2">{challenge.options.map((option,choice)=>{const answered=selected!==null;const correct=answered&&choice===challenge.correct;const wrong=answered&&choice===selected&&!correct;return <button key={`${challenge.id}:${option}`} disabled={answered} onClick={()=>choose(choice)} className={`min-h-14 rounded-xl border px-4 text-left text-sm font-black ${correct?'border-emerald-400 bg-emerald-400 text-black':wrong?'border-red-400 bg-red-400/10 text-red-200':'border-white/10 bg-black/25 active:border-fuchsia-400/60'}`}>{option}</button>})}</div>{selected!==null&&<div className="mt-4 rounded-xl border border-fuchsia-400/20 bg-fuchsia-400/[.05] p-4 text-xs leading-5 text-zinc-300"><b className={selected===challenge.correct?'text-emerald-300':'text-red-300'}>{selected===challenge.correct?`CORRECT · +${XP_BY_TIER[challenge.tier]} XP`:'MISSED · STREAK RESET'}</b><p className="mt-1">{challenge.explanation}</p><button onClick={next} className="mt-3 min-h-11 w-full rounded-xl bg-fuchsia-400 font-black text-black">{eliminated?'SEE RESULT':index===scenarios.length-1?'FINISH':'NEXT CHALLENGE'}</button></div>}</section></>}</div></div></ModalPortal>;
};
const Stat=({label,value}:{label:string;value:string})=><div className="rounded-xl bg-black/30 p-3"><div className="text-[8px] font-black text-zinc-600">{label}</div><div className="mt-1 text-xl font-black">{value}</div></div>;
